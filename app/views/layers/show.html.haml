.page-header
  %h1= @layer.nome

= simple_format @layer.description

.form-actions
  = link_to "Voltar", layers_path, :class => 'btn'
  = link_to "Editar", edit_layer_path(@layer), :class => 'btn'
  = link_to "Deletar", layer_path(@layer), :method => "delete", :data => { :confirm => "Tem certeza?" }, :class => 'btn btn-danger'
